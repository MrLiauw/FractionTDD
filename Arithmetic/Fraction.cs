namespace Arithmetic
{
    public class Fraction
    {
        public Fraction()
        {
            
        }

        public Fraction AddFraction(Fraction adder)
        {
            return new Fraction();
        }
    }
}
